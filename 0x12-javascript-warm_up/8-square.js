#!/usr/bin/node

const X = process.argv[2];
if (isNaN(X) {
  console.log('Missing size');
} else {
  for (let i = 0; i < X; i++) {
    for (let j = 0; j < X; i++) {
      consile.log(X);
    }
    console.log();
  }
}
